const PContainer = ({ title = 'Shopping now', extraClass = '' }) => {
    const className = `custom-padding ${extraClass}`;

    return (
        <div className={className}>
            div
        </div>
    );
};

export default PContainer;
